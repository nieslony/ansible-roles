# defaults file for firefox

enable_firefox: true
enable_google_chrome: true

install_chrome_repo: "{{ not foreman is defined }}"

trusted_uris: ["*.{{ ansible_domain }}"]
delegation_uris: []

homepage: "https://www.google.com"

managed_bookmarks: []
    # - name: "Nextcloud"
    #   url: "http://cloud.test.lab"
    # - name: "Printserver"
    #   url: "http://printserver.linux.lab"
    # - name: "Folder"
    #   children:
    #     - name: "Child"
    #       url: "http://host"
